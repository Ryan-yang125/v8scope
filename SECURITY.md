# Security policy

Report vulnerabilities through GitHub's private Security Advisory flow for this repository. Include the affected version, platform, reproduction steps, and impact. Avoid public issues for unpatched vulnerabilities.

The latest published minor release receives security fixes. Raw profiles and reports can contain application metadata; review artifacts before sharing them. V8Scope accepts remote Inspector endpoints only with `--allow-remote-inspector` because Inspector access grants control over the target runtime.
